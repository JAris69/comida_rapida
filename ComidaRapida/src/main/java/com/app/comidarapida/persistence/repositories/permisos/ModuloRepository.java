package com.app.comidarapida.persistence.repositories.permisos;

import com.app.comidarapida.persistence.entities.permisos.ModuloEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModuloRepository extends JpaRepository<ModuloEntity, Long> {
}
