package com.app.comidarapida.persistence.repositories.permisos;

import com.app.comidarapida.persistence.entities.permisos.AccionEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccionRepository extends JpaRepository<AccionEntity, Long> {
}
